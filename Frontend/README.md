
  # multimodal-anomaly-report-generation

  This is a code bundle for multimodal-anomaly-report-generation. The original project is available at https://www.figma.com/design/rfGxSqo9UnD90Nr8UmjkAx/multimodal-anomaly-report-generation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  